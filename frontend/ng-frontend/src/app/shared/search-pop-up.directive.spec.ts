import { SearchPopUpDirective } from './search-pop-up.directive';

describe('SearchPopUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchPopUpDirective();
    expect(directive).toBeTruthy();
  });
});
